"""Hex Tower MUD server package.

Version lives here rather than in packaging metadata because the server runs
from source and is never pip-installed. Keep it in step with
client/package.json and the git tag -- all three name the same release.
"""

__version__ = "0.0.1"
